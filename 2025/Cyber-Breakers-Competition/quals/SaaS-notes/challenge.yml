# yaml-language-server: $schema=https://raw.githubusercontent.com/dimasma0305/ctfify/refs/heads/master/function/template/templates/others/ctf-template/.gzctf/challenge.schema.yaml

name: "SaaS Notes"
author: "nyxmare"
description: |
  I am happy to anounce I made a Notes App. (This app is so buggy)

type: "DynamicContainer"
value: 1000

provide: "./dist"

container:
  flagTemplate: "CBC{vibe_coded_so_hard_to_the_point_i_dont_understand_this_notes_[TEAM_HASH]}"
  containerImage: "{{.slug}}:latest"
  memoryLimit: 512
  cpuCount: 3
  storageLimit: 512
  containerExposePort: 3000
  enableTrafficCapture: false

scripts:
  start: cd src && docker build -t {{.slug}} .
